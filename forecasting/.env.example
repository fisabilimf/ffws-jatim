# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=secret
DB_NAME=hydrodb

# Optional pool tuning
DB_POOL_SIZE=10
DB_POOL_RECYCLE=1800

# Base folder untuk file model/scaler (diprefix ke file_path relatif)
MODELS_BASE_DIR=/opt/models

# Flask
FLASK_ENV=production
